// See config directory for the knex configuration
// This file is used to point the knex CLI to the
// actual knexfile

const knexConfig = require('./config/knexfile');

module.exports = knexConfig;
